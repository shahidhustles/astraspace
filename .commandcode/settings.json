{
  "permissions": {
    "allow": [
      "Shell(bunx tsc -p packages/agent-core/tsconfig.json --noEmit 2 >& 1)",
      "Shell(bunx tsc -p chrome-extension/tsconfig.json --noEmit 2 >& 1)",
      "Shell(bun:*)",
      "Shell(bunx tsc --noEmit 2 >& 1)",
      "Shell(git:*)",
      "Shell(grep:*)",
      "Shell(bunx tsc -p packages/agent-core/tsconfig.json --noEmit)",
      "Shell(bunx tsc -p chrome-extension/tsconfig.json --noEmit)",
      "Shell(sed:*)",
      "Shell(cp:*)",
      "Shell(cd /Users/shahidpatel/codes/hackathons/astra-space-v2/packages/browser-mcp && python3 - <<'PY' import re p = \"extension-runtime/background.js\" s = open(p).read() # 1) Header comment + import s = s.replace( \"// Background service worker for Open Claude in Chrome extension.\\n// Handles: native messaging, CDP via chrome.debugger, tool dispatch, tab group management.\\n\\nimport * as humanize from \\\"./humanize/index.js\\\";\", \"// Background service worker for Open Claude in Chrome extension (Astra fork).\\n// Handles: WebSocket transport (replacing native messaging), CDP via chrome.debugger,\\n// tool dispatch, tab group management.\\n\\nimport * as humanize from \\\"./humanize/index.js\\\";\\nimport * as wsClient from \\\"./ws-client.js\\\";\", ) # 2) Replace NATIVE_HOST_NAME + nativePort with wsClient usage s = s.replace( \"const NATIVE_HOST_NAME = \\\"com.anthropic.open_claude_in_chrome\\\";\\n\\n// --- State ---\\nlet nativePort = null;\\nlet tabGroupId = null;\", \"// --- State ---\\nlet nativePort = null; // kept for sendResponse/sendError/save_to_disk parity\\nlet tabGroupId = null;\", ) # 3) Replace the whole connectNativeHost + start/stopHeartbeat + sendResponse/sendError + nativeRequest block start_marker = \"// --- Native messaging ---\\nfunction connectNativeHost() {\" end_marker = \"}\\n\\n// --- Proactive heartbeat\" # careful, use explicit # Find the exact block boundaries by locating unique anchors i = s.index(\"// --- Native messaging ---\") # The block runs until just before \"// --- Proactive heartbeat\" ... actually the heartbeat funcs are part of it. # Replace from \"// --- Native messaging ---\" up to (but not including) \"const pendingNative\" comment block start. j = s.index(\"// --- Native host request/response\") block = s[i:j] new_block = '''// --- WebSocket transport (replaces native messaging) --- function connectNativeHost() { if (wsClient.isWsConnected()) return; wsClient.initWsClient({ url: (typeof BROWSER_MCP_WS_URL !== \"undefined\" && BROWSER_MCP_WS_URL) || \"ws://localhost:8787/ws\", }); startHeartbeat(); } function startHeartbeat() { stopHeartbeat(); heartbeatTimer = setInterval(() => { if (!wsClient.isWsConnected()) return; wsClient.wsSend({ type: \"heartbeat\", t: Date.now() }); }, 15000); } function stopHeartbeat() { if (heartbeatTimer) { clearInterval(heartbeatTimer); heartbeatTimer = null; } } function sendResponse(id, result) { wsClient.wsSend({ id, type: \"tool_response\", result }); } function sendError(id, error) { wsClient.wsSend({ id, type: \"tool_error\", error: String(error) }); } ''' s = s[:i] + new_block + s[j:] # 4) Wire wsClient messages into handleToolRequest, preserving recording/screenshot/native handlers # Insert the onWsMessage listener right after the new block (before \"// --- Native host request/response\"). k = s.index(\"// --- Native host request/response\") listener = '''wsClient.onWsMessage((msg) => { if (msg.type === \"tool_request\" && msg.id) { handleToolRequest(msg.id, msg.tool, msg.args || {}); } else if (msg.type === \"recording_saved\") { const resolve = recorder.pendingSaves.get(String(msg.recording_id)); if (resolve) { recorder.pendingSaves.delete(String(msg.recording_id)); resolve(msg.ok ? msg.path : null); } } else if (msg.type === \"screenshot_saved\") { const pending = screenshotSaves.get(String(msg.id)); if (pending) { screenshotSaves.delete(String(msg.id)); if (msg.ok && msg.path) pending.resolve(msg.path); else pending.reject(new Error(msg.error || \"failed to save screenshot\")); } } else if (msg.id && pendingNative.has(msg.id)) { const p = pendingNative.get(msg.id); pendingNative.delete(msg.id); if (msg.ok) p.resolve(msg.result); else p.reject(new Error(String(msg.error || \"Native request failed\"))); } }); ''' s = s[:k] + listener + s[k:] # 5) Native host request/response block: keep pendingNative/nativeReqSeq/nativeRequest but # reimplement nativeRequest to reject cleanly (no host for write_temp_file yet). start2 = s.index(\"// --- Native host request/response\") end2 = s.index(\"// --- Screenshot helper\", start2) native_block = s[start2:end2] new_native = '''// --- Host request/response (staging; no host yet) --- // The fork used the native host for write_temp_file (upload_image staging). // With the native host removed, these requests cannot be served yet; reject // cleanly so the tool reports a clear error instead of hanging. const pendingNative = new Map(); // request id -> { resolve, reject } let nativeReqSeq = 0; function nativeRequest(msg) { return Promise.reject(new Error(\"native host not available in the WebSocket build: \" + (msg && msg.type))); } ''' s = s[:start2] + new_native + s[end2:] open(p, \"w\").write(s) print(\"patched OK\") PY grep -n \"wsClient\\|connectNativeHost\\|nativeRequest\\|sendResponse\\|sendError\" extension-runtime/background.js | head -30)"
    ],
    "deny": [],
    "defaultMode": "default"
  }
}