# Taste
- Prefers quick, minimal execution for tickets that are purely verification/proof work — explicitly asked to "make it quick" for a ticket that only tests whether existing functionality works, rather than wanting a lengthy plan or exhaustive ceremony. Confidence: 0.5
- Issues terse, imperative commands (e.g., "commit your changes", "commit the current state", "commit teh cahnges") and expects the agent to autonomously handle the complete underlying workflow — inspecting git state, staging (including unrelated pre-existing changes when the instruction is "commit the current state"), and writing the commit message — without further instructions. Reconfirmed a second time with "commit the current state", after which the agent summarized git state, ran `git add -A`, and authored a full conventional-commit message with no input from the user; a third time with "commit teh cahnges" following a run-verification. Confidence: 0.85
- Does not want a `Co-authored-by:` trailer on commits — explicitly said "do not co author" after a commit included it, and expects the agent to re-commit without it. Confidence: 0.9
- Prefers depending on established third-party packages over hand-rolled custom implementations, prioritizing "guarantees to work" reliability — called their custom browser control "a headache" and "shitty and won't work", and asked directly whether a third-party relay (browser-relay) is the way to go rather than building their own transport. Reaffirmed in later discussion: wants a prebuilt engine that "guarantees to work" in place of their own non-working code, while keeping everything else the same. Again reaffirmed when evaluating open-claude-in-chrome: said they're ready to drop the entire custom stack and adopt the open-source repo as the baseline (removing only its agent layer and keeping the rest, or just copying it wholesale). Confidence: 0.97
- Expects research on libraries and GitHub repos to use specific tooling: context7 for library documentation and the GitHub CLI (`gh`) for repo/README investigation — explicitly instructed "use context7 and github cli and read research" for comparing two technical approaches; again explicitly said "use context7 mcp" when researching Playwright's API. Confidence: 0.8
- When comparing competing technical approaches, wants the recommendation to include time/effort estimates for each option and an explicit assessment of whether each satisfies the stated requirements — asked "what would your recommendation give me time needed for both too. and if they satisfy my requirements." Confidence: 0.8
- Prioritizes low-friction deployment ("no headache") and dev/prod parity: a chosen solution must deploy cleanly and function identically in production as in development — "when I deploy the eve agent, it should be able to do it with no headache and it should function the same." Confidence: 0.7
- Product constraint for browser automation: the agent must operate on the user's real, logged-in browser session in the same instance — no headless mode, no fresh profile; new tabs open inside the user's existing browser so sessions/logins are preserved. Restated explicitly when the agent launched a debug Chrome: "we dont want a chrome launched in debugging mode we need actual browser." Confidence: 0.85
- The solution must plug directly into the user's existing Chrome via chrome.debugger with no external dependency, download, or setup the user has to do — product principle: the end user's only job is installing the Chrome extension and everything else is handled by the agent/backend, with nothing external for the user to download; restated the cloud product vision — Eve deployed in the cloud, the extension in the Chrome Web Store, and the user's only job is talking to the cloud Eve agent. Confidence: 0.95
- End-user product constraint: the shipped product must be a SINGLE standalone extension — the user objected to a setup requiring both a browser-control extension and the Astra chat extension ("astra space is a standalone package, user wont go ahead and install both right?"), i.e., browser control must be merged into the Astra Space extension itself; the two-extension arrangement is acceptable only as an internal testing convenience, never as the end-user install. Confidence: 0.85
ing stale elements after DOM changes, and auto-waiting for actionability — so their own agent tool layer (click/type/scroll/read) stays thin instead of hand-building those mechanisms. Confidence: 0.7
ms. Confidence: 0.7
 capability questions — asked "just tell me one thing: can Playwright control the user's already-open browser window?" — over long caveat-heavy framing; and acts decisively on the answer (immediately dropped Playwright once told it can't attach to the user's live window). Confidence: 0.6
- Prefers delegating open-ended research to an external AI (e.g., ChatGPT) via a self-contained prompt that describes the need generically with NO references to their existing codebase/system, so the researcher works independently and returns a drop-in package candidate. Also uses Claude (the product) as a reference model for architecture — wondered how Claude in Chrome handles the same agent-to-extension flow and asked the researcher to find its open-source equivalent. Confidence: 0.7
- Wants to brainstorm and vet a researched candidate together before committing to it — "the thing it finds we can go over it a brainstorm on if we can use it." Confidence: 0.6
- Prefers adopting an open-source baseline wholesale and only swapping the agent layer, rather than building or adapting — when evaluating open-claude-in-chrome (a clean-room reimplementation of Anthropic's official "Claude in Chrome" extension), offered to drop their entire custom stack, use it as the baseline, remove only its agent layer and add their own, "or we can just copy theirs" — keeping everything else identical. Confidence: 0.8
- Wants candidates validated against the actual source before any adoption decision — asked the agent to research a GitHub repo via context7/gh CLI and "tell me if it's the right thing we want," and the agent verified by cloning and inspecting the code: chrome.debugger attach, ref generation, coordinate resolution, tool list, and native-host transport. Confidence: 0.7
nt support ("then its even better dude"), and asked to verify against Eve's own docs/connections rather than building a custom bridge; reconfirmed when wiring Eve to the browser MCP — told the agent to read Eve's own MCP docs ("finally read the eve mcp docs") before implementing the connection, i.e., integrations should follow the framework's documented pattern (connection_search discovery, browser-mcp__* tool names), not assumed knowledge. Reconfirmed when creating an Eve skill: told the agent to "read the eve documentation on how to create a skill" and to make the model see and load it via the framework's native load_skill tool — framework artifacts (skills included) must follow the documented pattern and be wired through the framework's own discovery, not assumed knowledge. Confidence: 0.85
- Product architecture: "cloud brain, local hands" with an outbound-only connection — the user's extension dials OUT to the cloud (WebSocket) so no inbound firewall hole is needed; the hosted MCP server acts purely as a router mapping a tool call to the right user's connected session; browser control is deliberately decoupled from the agent (Eve → hosted MCP → WebSocket → extension → chrome.debugger → user's tab), never Eve talking to Chrome directly — explicitly confirmed: "the connection won't be between eve and chrome for browser control, it would be between mcp and users chrome instance." Confidence: 0.85
- Prefers testing MCP servers via curl over HTTP as the primary verification method, on the reasoning "if it works on http it works anyhow" — HTTP is transport-agnostic, so a curl-verified contract will work from any client; proposed the end goal that the agent itself can call the server's tools via curl. Confidence: 0.7
- Explicitly stops the agent from starting implementation until the approach is agreed through discussion — "do not start doing it am just asking"; wants a proposal first and will give the go-ahead for execution separately. Reconfirmed when the merge was proposed and the user replied with a terse "yes go ahead merge it" — no additional detail or ceremony, just explicit approval to proceed. Confidence: 0.7
- Once a plan is approved, wants direct execution from the saved plan file (read it first, implement step by step, refer back to it) — explicitly said the plan is "already approved" and told the agent NOT to enter plan mode or call exit_plan_mode to re-present it. Prefers execution to proceed without re-negotiating an agreed plan. Confidence: 0.7
- Prefers vendoring open-source baselines as in-repo packages (e.g., clone open-claude-in-chrome into `packages/browser-mcp`) and making modifications inside the monorepo, rather than depending on them externally. Confidence: 0.6
- Prefers a dual-track, non-disruptive adoption strategy when swapping in a new implementation: keep the current implementation untouched ("unattached") while building and testing the new path in parallel, and only do the replacement after the new one is proven to work — explicitly approved "keep the current implementation we have unattached, let's test if the mcp works fine and then we will do the replacement." Confidence: 0.8
- Wants plans written into the repo's docs directory (asked the agent to "lay out a plan inside the docs") rather than kept only in ephemeral plan files. Confidence: 0.5
- Prefers validating against the real end-to-end path before integrating with the top-level consumer: when adding a browser-control MCP server, asked to "connect it first with the browser and then do curl testing, before we connect it to eve" — i.e., prove the extension↔browser↔server chain with real curl calls first, then wire it into the agent. Incremental, layer-by-layer verification against live components rather than assuming simulated tests are sufficient. Confidence: 0.8
- Prefers to perform the browser-side setup himself in his real browser (loading the unpacked extension from the package) and have the agent handle the server-side testing, rather than the agent launching a debug-mode Chrome instance — "i will load it into my browser ... and then you can test, cause thats what we want right, we dont want a chrome launched in debugging mode we need actual browser." Confirmed again by following through: loaded the extension himself and opened a real page (amazon.in) in his current tab, then handed off to the agent to curl-test against his live browser. Reconfirmed during the Amazon cart workflow: each time the agent patched the vendored extension's background.js, the user promptly reloaded it himself in chrome://extensions and handed the browser back for the agent to re-test — again confirmed when he replied "relaoded" and immediately handed off for the agent to re-run the test. Confidence: 0.9
- Product behavior for the browser agent: wants BOTH modes supported simultaneously — the agent can get context from and act on the user's existing open tabs AND create its own tab groups — not just an agent-owned tab group. Explicitly corrected the single-mode design: "i actually wanted to use the users tab but also create our own like it does right now... support for both, getting context from the existing open tabs and be able to create new groups." Confidence: 0.9
- Agrees with (and endorses) the finding that reliable agent input actions (clicks) should run in the agent's own group tab — the deep Amazon test proved clicks on the user's pre-existing tab are dropped (0 events fired even with focus emulation), while the same click works instantly on a group tab — so the agent's own tab is the dependable path for acting, and user tabs are for reading/context. Confidence: 0.6
- Acceptance test for browser-automation changes: wants the feature proven with a complete real-world user workflow that ends in a state change — e.g., a full Amazon flow (search → open product → add to cart) against the real browser — rather than isolated read/click tool checks. "test it with a complete amazon workflow, add a product in the cart." Reconfirmed when validating the accessibility-tree/screenshot fixes: the user's chosen next test was another fresh real-world task — "i ll ask it to fill a form this time" — a workflow that ends in a visible state change, rather than tool-level checks. Confidence: 0.8
- Wants the agent to verify the latest run's trace for redundancy BEFORE committing a fix — asked "check the last run, it was perfect, see if its doing anything redundant. if alls correct commit teh cahnges": a post-fix commit should be gated on inspecting the actual run (tool-call sequence, duplicate calls, single final response, no redelivery errors) and only committed once proven clean. Confidence: 0.7
- After a bug fix, wants the MCP tool surface checked ("check what tools are connected to eve... try the same task with mcp tools, and check if it works according to us") — but clarified that this means reading the exposed tool list and testing each tool via direct curl calls against the MCP server, NOT running the Eve agent itself ("no no, dont run eve"). Verifying through the actual agent is a separate, later step; the tool layer itself is validated at the curl level. Confidence: 0.5
ools are connected/discoverable to it, then the same real-world task re-run through the agent's own tool use ("try the same task with mcp tools, and check if it works according to us") — confirming the fix works end-to-end through the actual consumer of the tools, not just via curl-level tests. Confidence: 0.7
- For destructive cleanup tasks (deleting an old implementation), wants a written plan first that enumerates exactly what gets deleted vs. kept across the affected areas (agent, extension, packages) before any execution — explicitly asked "actually plan on it. what things need to be deleted before we setup the tools" when requesting removal of the old browser-control stack. Confidence: 0.7
- Sequencing preference: wants a cleanup phase that removes the superseded implementation entirely before wiring the new tooling into the top-level consumer — asked to delete all the old browser-control setup "before we setup the tools with eve" (i.e., clean tree first, then integrate the new MCP tools). Confidence: 0.6
- After wiring an integration, wants the end-to-end chain proven through the real consumer before declaring it done — for the Eve↔browser-mcp connection, the agent verified by actually invoking the Eve agent and having the model call `connection_search`, discover the `browser-mcp__*` tools, and list live browser tabs through them (not just manifest/config checks). Acceptance = the model itself successfully uses the integrated tool path against live components. Confidence: 0.6
- Product/test workflow for the browser stack requires TWO extensions loaded in Chrome for a full flow: the browser-control extension (vendored `packages/browser-mcp/extension-runtime/`, connects to the MCP server) plus the Astra chat/side-panel extension — the agent's job is to confirm both are built and connected (dist present, MCP server + eve dev server up, tabs_context returning real tabs) and hand the load step to the user. Confidence: 0.6
- When the user reports symptoms in agent runs (slowness, duplicate tool fires, double reasoning, duplicate final responses), expects a root-cause investigation that distinguishes whether it is a model issue or an infra/framework issue — the agent proved via the dev log (`Queue delivery failed / fetch failed` redelivery counts) and trace-level duplicate tool calls (identical name+args firing 2-3×) that Eve's durable workflow engine was redelivering/re-executing the same turn, i.e., an Eve dev-mode infra bug, NOT their own browser-mcp code and NOT the model retrying. The user's phrasing "is it a model issue or did it face any problems" shows they want the blame attributed correctly before any fix. Confidence: 0.7
- Same blame-attribution expectation extends to model-competence vs tool-failure: when an agent run loops on a task (e.g., repeatedly re-navigating to the same Amazon product without completing add-to-cart), the user asks outright "is it a model competence issue or are the tools not working right?" — and the accepted diagnostic method is to settle it empirically: pull the run's tool-call sequence and the model's own reasoning from the trace, then directly re-execute the tools (read_page/find/scroll/click on the same page) to prove the tools work, so the loop is correctly attributed to model strategy rather than blamed on (or "fixed" in) the working tooling. Confidence: 0.7
- When debugging lingering issues in Eve agent runs, expects the agent to go into the actual run/trace logs stored in Eve's folder (`.eve/...`) and read the model's own messages and reasoning to find the real problems — explicitly told the agent to "read the last run from eve logs... read what the ai was saying and you will find it" — rather than only scanning for error spans: the AI's own words surfaced issues (intermittent "Could not generate accessibility tree" failures, blurry screenshots, empty product lists, a click that didn't navigate, queue delivery failures) even though every tool call reported success. Reconfirmed when reporting a slow form-fill run with duplicate tool firings, double reasoning, and duplicate final responses: told the agent to "read the logs" and investigate whether it was a model issue or "eve doing something fishy under the hood" — the agent confirmed via dev-log redelivery counts and trace-level counts of identical tool calls with identical args. Confidence: 0.9
ext returning real tabs) and hand the load step to the user. Confidence: 0.6
uccess. Confidence: 0.8
ext returning real tabs) and hand the load step to the user. Confidence: 0.6
- Prefers to control the dev server lifecycle himself: when a fix required restarting the eve dev server, told the agent "just stop the server i will run it myself" (and noted he'd already cleared the cache) — the agent should stop a server on request but leave starting/restarting to the user. Confidence: 0.6
- After diagnosing a bug as an upstream framework issue, wants the agent to verify whether it's actually fixed in the newer version before recommending an upgrade — "i want you to see if it is fixed in the new version, cause it would be a big problem in the future runs"; the agent confirmed via the upstream GitHub releases (exact fix found in eve 0.45.2) rather than upgrading blindly. Confidence: 0.7
- After consolidating/merging code into one location, expects the superseded copy to be fully removed (single source of truth) and proactively audits for any other dead code no longer used — asked "did you also clear the browser-mcp's extension now cause we are using the runtime in astra space itself. and check for any other redundant thing which we are not using but still exists"; the resulting audit-and-remove pass (dead imitation-learning recorder ~640 lines, 7 no-op/native-host-dependent MCP tools 26→19, stale manifest permissions, smoke test fix) was executed and committed directly without objection, i.e., cleanup after a merge is expected to be thorough, not just the specific item named. Confidence: 0.7
- When codifying a workflow into an agent skill, wants the agent to FIRST run the exact flow live against the real system (verify it works, including verification of every step — e.g., scroll target confirmed via `elementFromPoint` hit-check) and only THEN write the skill that encodes those proven steps — told the agent "first you go ahead and try putting a product to cart. and then create a skill for eve to do the same"; the skill was written to capture the empirically-verified recipe rather than assumed knowledge. Confidence: 0.8
- End-to-end acceptance for framework artifacts (Eve skills): after writing a skill, expects proof that the framework actually discovers it and the model can load it — checked `eve info` for the skill count, inspected the discovery manifest for the skill's description + full markdown, and verified it's advertised via the framework's `load_skill` tool before declaring done. Confidence: 0.7
